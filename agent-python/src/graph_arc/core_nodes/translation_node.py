"""
Translation Node
Description: Translates final reply into user's language.
"""

def translate_reply(reply: str, language: str) -> str:
    """
    Returns translated reply in the user's language.
    """
    # Placeholder: Integrate with translation API
    return f"[{language}] {reply}"
